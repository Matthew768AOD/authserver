<Window x:Class="Quotes.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quotes" Height="450" Width="800">
    <DockPanel Margin="8">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Name="AddBtn" Content="Add" Width="80" Click="AddBtn_Click" Margin="4"/>
            <Button Name="EditBtn" Content="Edit" Width="80" Click="EditBtn_Click" Margin="4"/>
            <Button Name="DeleteBtn" Content="Delete" Width="80" Click="DeleteBtn_Click" Margin="4"/>
        </StackPanel>

        <DataGrid Name="QuotesGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Text" Binding="{Binding Text}" Width="*"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
